%     Written by Sigal Saar 10.25.05
%==============================================================

%=========Mouse callback===========
function []=get_pnts(src, e , x_length , y_length)
  curr_point=get(gcf,'CurrentPoint');
  x_val=curr_point(1)*x_length
  y_val=curr_point(2)*y_length
  pause
return